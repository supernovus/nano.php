{
  "desc": "  Installs into a single webroot folder.\n  Uses SQL database models (with SQLite as example DB.)\n  Uses older techniques and libraries such as the depreciated Dispatch plugin.\n  Doesn't use translations, or anything advanced.",
  "base": "approot",
  "appname": "ExampleApp",
  "nano.js":
  {
    "grunt":   false,
    "modules": false,
    "scripts": "scripts",
    "styles":  "style"
  },
  "postinstall": "cd $TARGET && mkdir db && touch db/staging.sqlite"
}